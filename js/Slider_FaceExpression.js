class Slider_FaceExpresion{
    constructor(name, number) {
        this.name = name;
        this.number = number;
    }

    Point_ChangeFace = (point) => {
        console.log("point",point);
    }
}